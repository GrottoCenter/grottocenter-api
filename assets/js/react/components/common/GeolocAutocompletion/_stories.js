import * as React from 'react';
import { storiesOf } from '@storybook/react';
import Example from './index';

storiesOf('Example autocomplete geoloc', module).add('Default', () => (
  <Example />
));
